import { MostarocultarpassPipe } from './mostarocultarpass.pipe';

describe('MostarocultarpassPipe', () => {
  it('create an instance', () => {
    const pipe = new MostarocultarpassPipe();
    expect(pipe).toBeTruthy();
  });
});
